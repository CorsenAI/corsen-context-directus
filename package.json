{
  "name": "corsen-context-directus",
  "version": "1.0.0",
  "private": true,
  "description": "Deployable WebMCP and MCP bridge for published Directus content.",
  "license": "MIT",
  "author": "Corsen AI",
  "repository": "github:CorsenAI/corsen-context-directus",
  "homepage": "https://directus-webmcp.corsen.ai",
  "bugs": "https://github.com/CorsenAI/corsen-context-directus/issues",
  "keywords": ["webmcp", "mcp", "directus"],
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:env": "node --env-file=.env server.js",
    "test": "node --check server.js && node scripts/test-integration.mjs"
  },
  "dependencies": {
    "@corsenai/corsen-context": "^2.0.1",
    "express": "^5.0.0"
  },
  "overrides": {
    "@corsenai/corsen-context": {
      "fast-xml-parser": "^5.7.0"
    }
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
